-- app_owner role tests (stub — expand as ownership checks are defined)
SELECT * FROM no_plan();

-- placeholder: ensure app_owner role exists
SELECT has_role('app_owner', 'role app_owner exists');

SELECT * FROM finish();
